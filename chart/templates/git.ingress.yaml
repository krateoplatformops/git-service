apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: git-ingress
  labels:
  annotations:
    kubernetes.io/ingress.class: 'kong'
spec:
  rules:
    - host: {{ .Values.git.host }}
      http:
        paths:
          - path: /*
            pathType: Prefix
            backend:
              service:
                name: git-svc
                port:
                  number: 80
